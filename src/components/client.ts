
import { getClients, createClient, updateClient, deleteClient, Client } from '../services/databaseApi';

/**
 * Re-exports the database API functions for clients to maintain compatibility
 * with the existing codebase.
 */
export { getClients, createClient, updateClient, deleteClient, Client };
